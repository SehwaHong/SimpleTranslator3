// import { saveTranslation, clearTranslationHistory, loadHistory } from "./db";
// const { saveTranslation, clearTranslationHistory, loadHistory } = require("./db");

//Testing database using Jest


//imports just don't work for some reason and its annoying

describe("testing insertion", () => {
    test("inserting one insertion", () => {
        expect(1+2).toBe(3);
    });
});